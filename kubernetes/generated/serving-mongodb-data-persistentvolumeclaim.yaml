apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: serving-mongodb-data
  name: serving-mongodb-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
