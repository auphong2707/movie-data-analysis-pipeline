apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: speed-kafka-broker1-data
  name: speed-kafka-broker1-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
