apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: batch-postgres-data
  name: batch-postgres-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
