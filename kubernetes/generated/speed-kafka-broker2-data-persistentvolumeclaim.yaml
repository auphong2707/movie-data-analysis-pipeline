apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: speed-kafka-broker2-data
  name: speed-kafka-broker2-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
