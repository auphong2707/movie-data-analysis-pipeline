apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: serving-prometheus-data
  name: serving-prometheus-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
