apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: serving-grafana-data
  name: serving-grafana-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
