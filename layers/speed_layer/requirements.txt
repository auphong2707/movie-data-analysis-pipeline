# =====================================================================
# Speed Layer Python Dependencies
# =====================================================================
# IMPORTANT: Versions must match Dockerfile installations
# Base: Python 3.10 + Java 11 + Spark 3.4.4
# =====================================================================

# ---------------------------------------------------------------------
# Kafka & Streaming
# ---------------------------------------------------------------------
# kafka-python: Native Python client cho Kafka
# confluent-kafka: Kafka producers với Avro serialization (for tmdb producer)
# avro: Schema serialization/deserialization
# fastavro: Required by confluent-kafka for Avro serialization
kafka-python==2.0.2
confluent-kafka==2.3.0
avro==1.11.3
fastavro==1.9.4

# ---------------------------------------------------------------------
# PySpark & Streaming (EXACT VERSION - Must match Dockerfile!)
# ---------------------------------------------------------------------
# PySpark 3.4.4: Compatible với Java 11, Spark packages 3.4.x
pyspark==3.4.4

# ---------------------------------------------------------------------
# Cassandra Database Driver
# ---------------------------------------------------------------------
# cassandra-driver 3.29.1: Compatible với Cassandra 4.x
cassandra-driver==3.29.1

# ---------------------------------------------------------------------
# Sentiment Analysis
# ---------------------------------------------------------------------
# vaderSentiment: Real-time sentiment analysis cho reviews
vaderSentiment==3.3.2

# ---------------------------------------------------------------------
# HTTP & API
# ---------------------------------------------------------------------
# requests: HTTP client cho TMDB API
requests==2.31.0

# ---------------------------------------------------------------------
# Configuration & Environment
# ---------------------------------------------------------------------
# PyYAML: YAML config parser
# python-dotenv: Load environment variables từ .env
pyyaml==6.0.1
python-dotenv==1.0.0

# =====================================================================
# Note: Dependencies như pandas, numpy KHÔNG được cài trong Dockerfile
# để giữ image size nhỏ. Chỉ thêm nếu thực sự cần thiết.
# =====================================================================
