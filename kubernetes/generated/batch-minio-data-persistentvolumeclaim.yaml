apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: batch-minio-data
  name: batch-minio-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
